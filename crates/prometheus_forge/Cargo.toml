[package]
name = "prometheus_forge"
version = "1.1.0"
edition = "2021"
license = "MIT"
description = "PrometheusForge — Hyper-Divine Fire Innovation Crucible with Full Async Integration"

[dependencies]
nexi = { path = "../" }
grok_arena_pinnacle = { path = "../crates/grok_arena_pinnacle" }
futarchy_oracle = { path = "../crates/futarchy_oracle" }
whitesmiths_anvil = { path = "../crates/whitesmiths_anvil" }
tokio = { version = "1.38", features = ["full", "rt-multi-thread", "macros"] }  # Full async runtime
