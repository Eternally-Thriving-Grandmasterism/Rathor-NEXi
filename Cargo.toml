[package]
name = "nexi"
version = "1.0.1"
edition = "2021"
license = "MIT"
description = "NEXi — The Lattice That Remembers. Mercy. Truth. Memory. Joy."
repository = "https://github.com/Eternally-Thriving-Grandmasterism/NEXi"

[dependencies]
pyo3 = { version = "0.20", features = ["extension-module"] }
hex = "0.4"
rand = "0.8"

[lib]
crate-type = ["cdylib", "rlib"]
