apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: triton-model-repo-pvc
  namespace: rathor-inference
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
  storageClassName: standard  # adjust to your cluster's class (longhorn, nfs, etc.)
